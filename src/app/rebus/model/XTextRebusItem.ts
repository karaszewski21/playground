import { IRebusItem } from './interface/IRebusItem';

export class XTextRebusItem implements IRebusItem {
  width: number;
  nameComponent: string;
  name: string;
  height: number;
  size: number;
  font: string;
}
